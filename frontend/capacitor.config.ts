import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.foodrecommend.app',
  appName: 'FoodRecommend',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
